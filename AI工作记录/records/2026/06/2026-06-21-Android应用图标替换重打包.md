# Android 应用图标替换重打包

## 背景

- 目标：重新打包 Android 客户端，并把 app 图标切换为电脑端 Codex 的图标。
- 最终正确图标来源：`C:\Program Files\WindowsApps\OpenAI.Codex_26.506.3741.0_x64__2p2nqsd0c76g0\assets\Square44x44Logo.targetsize-256_altform-unplated.png`。

## 已实施改动

- 生成 Android launcher 所需的 `mipmap-mdpi`、`mipmap-hdpi`、`mipmap-xhdpi`、`mipmap-xxhdpi`、`mipmap-xxxhdpi` PNG 资源。
- 将 `android/app/src/main/AndroidManifest.xml` 中的 `android:icon` 和 `android:roundIcon` 从系统默认图标改为 `@mipmap/ic_launcher`。

## 后续纠正

- 首次使用的 `C:\Users\hetao\AppData\Local\OpenAI\Codex\bin\codex.exe` 是 CLI 侧可执行文件，提取到的是默认 EXE 图标，不是桌面 Codex 应用图标。
- 已改用 Windows 应用包中的真实桌面图标资源：`C:\Program Files\WindowsApps\OpenAI.Codex_26.506.3741.0_x64__2p2nqsd0c76g0\assets\Square44x44Logo.targetsize-256_altform-unplated.png`，并重新生成 Android 各密度 `ic_launcher.png`。

## 验证结果

- `powershell -ExecutionPolicy Bypass -File .\scripts\build-android-debug.ps1`：通过，生成 `android/app/build/outputs/apk/debug/app-debug.apk`，时间戳 `2026/6/21 10:15:51`。
- `cd android; .\gradlew.bat testDebugUnitTest`：通过。
- 纠正图标来源后重新执行 `powershell -ExecutionPolicy Bypass -File .\scripts\build-android-debug.ps1`：通过，重新生成 `android/app/build/outputs/apk/debug/app-debug.apk`，时间戳 `2026/6/21 12:02:38`。
- 纠正图标来源后重新执行 `cd android; .\gradlew.bat testDebugUnitTest`：通过。
- `aapt dump badging android/app/build/outputs/apk/debug/app-debug.apk`：确认 APK 内 `application-icon` 指向各密度 `res/mipmap-*/ic_launcher.png`。
